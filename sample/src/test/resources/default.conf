url {
    backend = "http://localhost:10010"

    rest = rest
    ping = ping
    simple = simple
    error-http-codes = errorHttpCodes
    person = person
    complex-json = complexJson
}

rest {
    base-url = "http://localhost:10010"
}

db {
    test = ${h2} { url = "jdbc:h2:mem:test" }
}
