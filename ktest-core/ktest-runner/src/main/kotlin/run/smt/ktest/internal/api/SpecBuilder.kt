package run.smt.ktest.internal.api

/**
 * Idea behind this maker:
 * There are internal-like functions in `BaseSpec` that allow you to access some raw
 * functionality (like addition of raw suites/cases) through methods bound to this
 * marker object.
 */
object SpecBuilder // marker object
